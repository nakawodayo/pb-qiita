## Qiita Organization 記事一覧・検索アプリ 要件定義（MVP）

### 目的
- **特定のQiita Organizationに含まれる記事**を、ブラウザから**一覧表示・検索**できるようにする。
- はじめは**閲覧専用（Read-only）**の最小機能で公開し、段階的に拡張する。

### 対象ユーザー
- 特定Organizationのメンバー/関係者、またはその記事を追いたい閲覧者。

---

### スコープ（MVP）
- **Organization指定**: 設定値で1つのOrganizationを固定（例: `ORG_ID`）。
- **記事一覧**: 公開記事（Public）を取得・表示。
- **検索**:
  - キーワード全文検索（タイトル・本文）
  - タグでのフィルタ
  - 投稿者でのフィルタ（オプション）
- **ソート**: 新着順（投稿日降順）を既定。将来、いいね数順など追加。
- **ページネーション**: 20件/ページ（変更可能）。
- **記事詳細遷移**: クリックでQiita本体の記事URLへ遷移（アプリ内詳細ページは持たない）。
- **エラー表示**: APIエラー/レート制限時にユーザー向けメッセージ表示。

除外（MVPではやらない）:
- 組織横断検索、ブックマーク、履歴、通知機能
- 記事の作成・編集・削除
- アプリ内での記事詳細レンダリング（直リンクのみ）

---

### 画面要件
1) 一覧画面（トップ）
- 検索バー（プレースホルダ: 例「キーワード、タグ:react、user:alice」）
- 絞り込みUI（タグ、ユーザーは簡易セレクタ or クエリ式）
- ソートドロップダウン（MVPは非活性 or 新着固定）
- 記事リスト（タイトル、著者、投稿日、いいね数、タグの一部、抜粋）
- ページネーション（前へ/次へ、ページ番号）
- フッターにレート制限・最終更新時刻（キャッシュ更新）を薄く表示

レスポンシブ:
- モバイル/タブレット/PCで崩れないレイアウト（1カラム基調）

アクセシビリティ:
- キーボード操作可能、適切な代替テキスト、コントラスト確保

---

### 機能要件
- 記事取得:
  - Qiita API v2 を使用
  - エンドポイント候補:
    - `GET /api/v2/organizations/{organization}/items`
    - 必要に応じて `GET /api/v2/tags`, `GET /api/v2/users/{id}` など
- 検索・フィルタ:
  - クエリパラメータでサーバ/クライアントいずれかからAPIへ
  - 可能な限りQiitaのクエリ機能を活用し、足りない場合はクライアント側フィルタ
- ページネーション:
  - APIの `page`, `per_page` を使用（既定 `per_page=20`）
- クリック時:
  - 新規タブでQiita記事URLを開く

---

### 非機能要件
- パフォーマンス:
  - 初回表示 2秒以内（ネットワーク状況に依存）
  - 画像は遅延読み込み
- 信頼性:
  - API失敗時の再試行（1回）と明瞭なエラーメッセージ
- スケーラビリティ:
  - クライアントのみ構成（MVP）。将来は中間キャッシュ層追加を想定
- セキュリティ:
  - アクセストークンは環境変数注入（ビルド時/サーバ側プロキシ経由）で秘匿
  - ブラウザへ秘匿値を配布しない方針（必要ならサーバサイド導入）
- ログ/モニタリング:
  - コンソール最低限。将来外部サービス連携

---

### 外部APIとレート制限
- Qiita API v2:
  - 認証なしでも取得可だがレートが厳しいため、**パーソナルアクセストークン**利用を推奨
  - レートヘッダ（`Rate-Remaining`, `Rate-Reset`）を読取り、UIに簡易表示
  - `If-None-Match`（ETag）や `Cache-Control` を活用し、条件付き取得を検討

---

### 設計方針（MVP）
- フロントエンド: React or Next.js（SSG/SSRは後から選択可能）／シンプルなSPAでも可
- データ取得:
  - 直接Qiita API呼び出し（MVP）
  - 可能なら**薄いプロキシサーバ**（Node/Cloud Functions）でトークン秘匿と簡易キャッシュ
- 状態管理:
  - まずはURLクエリ＋軽量なローカル状態（React Query等でフェッチとキャッシュ）

---

### 環境変数（例）
- `VITE_QIITA_ORG_ID` or `NEXT_PUBLIC_QIITA_ORG_ID`（公開可）
- `QIITA_ACCESS_TOKEN`（公開不可、プロキシ側で保持）

---

### 受け入れ基準（MVP）
- Organizationを設定してビルド・起動すると、記事一覧が表示される
- 検索キーワードでタイトル/本文が絞り込まれる（APIクエリ連携）
- タグ指定で記事が絞り込まれる
- 20件/ページでページ移動が可能
- エラー時にユーザー向けのメッセージが表示される
- 記事クリックでQiitaの該当記事が新規タブで開く

---

### 将来拡張
- 高度な並び替え（LGTM数、更新日、閲覧数など）
- 組織メンバー一覧・著者フィルタUI
- サーバサイドキャッシュ/検索インデックス
- タグサジェスト/オートコンプリート
- ブックマーク、共有リンク、エクスポート（CSV/JSON）
